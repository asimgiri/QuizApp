import React from 'react';

export default function Top(){
    return(
        <div className="top">
            <button className="right"><i className="fas fa-sign-out-alt"></i> Logout</button>
        </div>
    )
}